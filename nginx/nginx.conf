upstream django_app {
    server web:8000;
    keepalive 32;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 100M;

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        access_log off;
    }

    location /media/ {
        alias /app/media/;
        expires 30d;
        access_log off;
    }

    location / {
        proxy_pass http://django_app;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_connect_timeout 60s;
        proxy_read_timeout 60s;
    }
}






# upstream django_app {
#     server web:8000;
# }

# server {
#     listen 80;
#     server_name shagorrobidas.dev www.shagorrobidas.dev 51.20.17.15;

#     location / {
#         proxy_pass http://django_app;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host $host;
#         proxy_redirect off;
#     }

#     location /static/ {
#         alias /app/staticfiles/;
#     }

#     location /media/ {
#         alias /app/media/;
#     }

#     client_max_body_size 100M;
# }
